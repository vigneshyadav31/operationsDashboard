{
  "name": "operations-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "The Operations Dashboard — a production-grade Founder's Office full-stack app.",
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -k -n SERVER,CLIENT -c blue,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "cross-env NODE_ENV=production node server/src/index.js",
    "test": "cd server && node --test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  }
}
